---
name: ai-decision-evidence-auditor
description: Audits 当用户需要核验 AI 生成报告、研究结论或高风险决策材料的证据链，逐项追溯主张、来源、时效与不确定性，并输出可执行的放行、复核或阻断结论。
---

# AI 决策证据链审计师（AI Decision Evidence Auditor）

## 核心定位

把“读起来很专业”的 AI 报告拆成可核验主张，检查每条主张是否有直接来源、是否被来源真实支持、是否仍在有效期内，以及它对决策的影响。适用于研究简报、竞品报告、政策摘要、投标材料和高风险业务判断；不替代法律、医疗、财务或安全领域的持证专家，也不为缺证据的结论背书。

## 触发场景

- “帮我审计这份 AI 报告的证据链，哪些结论不能直接用？”
- “把研究结论逐条对应到来源，给出放行、复核、阻断意见。”
- “Audit the evidence chain in this AI-generated decision memo.”
- 只需润色、摘要或改写文风时不触发；先转用写作类技能。

## 工作流

### Step 1：冻结决策边界

记录材料版本、审计时间、预期决策、允许的风险等级和责任人。把“提供信息”和“据此采取不可逆行动”分开。若用户没有说明决策用途，按最低授权处理：只做只读审计，不替用户执行采购、发布、封禁、投资或对外沟通。

### Step 2：建立主张台账

逐段提取可能影响决策的事实主张，写入 JSON：`id`、`claim`、`impact`、`source_url`、`source_title`、`published_at`、`accessed_at`、`quote_or_locator`、`confidence`。观点、预测和事实分开；复合句拆成可单独验证的原子主张。禁止用 AI 自己的总结作为最终来源。

### Step 3：评估来源与蕴含关系

优先级为官方原文/原始数据 > 同行评审或一手采访 > 主流媒体 > 聚合与社交转述。打开来源，确认标题、日期、作者和定位信息，并判断来源是直接支持、部分支持、仅相关还是矛盾。登录墙或付费墙导致无法读取时标为 `unverified`，不得根据搜索摘要猜正文。

### Step 4：检查时效、独立性与缺口

检查规则、价格、版本、人员、时间窗是否可能变化；同一通讯稿的多次转载只算一个来源。高影响主张至少需要一个一手来源；涉及不可逆或高风险行动时，要求第二个独立证据或明确的人类复核。找不到证据时写 `missing`，不能用常识补齐。

### Step 5：运行一致性检查

将台账保存为 `claims.json`，运行 `python scripts/audit_claims.py claims.json --out audit-report.md`。脚本仅做结构与规则检查，不访问网络，也不判断来源内容真假。若出现缺字段、未来日期、重复 URL 或高影响主张缺直接来源，先回到 Step 2-4 修复；不能为了通过而降低 impact。

### Step 6：给出决策门槛

逐条给出 `PASS`、`REVIEW`、`BLOCK`：证据直接且时效合格可 PASS；部分支持、来源受限或需要专家解释为 REVIEW；核心主张缺证、矛盾或来源不可追溯为 BLOCK。整体结论服从最高风险缺口，不用多数票掩盖一个关键阻断项。

### Step 7：交付证据包

输出 `claims.json`、`audit-report.md`、`source-log.md` 和 `decision-note.md`。decision-note 必须说明可安全采用的内容、不可采用的内容、需要谁在何时补什么证据，以及本次审计未覆盖什么。敏感原文只记录授权位置和哈希，不复制到公开仓库。

## 输出格式

```markdown
# 决策证据链审计
结论：REVIEW

| ID | 原子主张 | 影响 | 来源支持 | 时效 | 结论 |
|---|---|---|---|---|---|
| C1 | 产品将于 9 月 30 日停止旧接口 | high | 官方公告直接支持 | 2026-09-18 查阅 | PASS |
| C2 | 所有客户必须立刻迁移 | high | 公告未支持“所有” | — | BLOCK |

下一步：产品负责人在上线前取得客户范围清单；未取得前禁止批量迁移。
```

## Gotchas

1. 多个转载链接不等于多个独立来源；追到共同原文后只计一次。
2. 搜索结果摘要可能截断限定词；未打开正文只能标记 `unverified`。
3. “来源存在”不等于“来源支持主张”；必须记录可定位段落并判断蕴含关系。
4. 发布日期新不代表数据新；同时检查数据覆盖期和材料更新时间。
5. 高置信语气不是证据；confidence 记录的是审计者对证据关系的判断，不是模型口吻。
6. 高风险结论有一条核心 BLOCK 就不能整体 PASS；不得用平均分稀释阻断项。
7. 登录、隐私和商业机密边界优先；不要绕过访问控制，也不要把敏感原文提交到公开仓库。
8. 脚本只验证台账结构与规则，不验证网页真伪；最终判断必须保留人工来源阅读记录。
